services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - sqlite_data:/app/data
    restart: always
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always

volumes:
  sqlite_data:
